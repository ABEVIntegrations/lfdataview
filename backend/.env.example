# Database Configuration
DATABASE_URL=postgresql://lfdataview:changeme_strong_password@postgres:5432/lfdataview

# Laserfiche OAuth Configuration
# Register your app at https://developers.laserfiche.com/
LASERFICHE_CLIENT_ID=your_client_id_here
LASERFICHE_CLIENT_SECRET=your_client_secret_here
LASERFICHE_REDIRECT_URI=http://localhost:8000/auth/callback

# Security Keys
# Generate SECRET_KEY with: openssl rand -hex 32
SECRET_KEY=your_secret_key_here_use_openssl_rand_hex_32

# Generate TOKEN_ENCRYPTION_KEY with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
TOKEN_ENCRYPTION_KEY=your_fernet_key_here

# Application Settings
ENVIRONMENT=development
DEBUG=true
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Session Configuration
SESSION_EXPIRY_DAYS=7
